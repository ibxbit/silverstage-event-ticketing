<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eaglepoint.venue.mapper.UserNotificationMapper">

    <resultMap id="UserNotificationMap" type="com.eaglepoint.venue.domain.UserNotification">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="message" column="message"/>
        <result property="notificationType" column="notification_type"/>
        <result property="readFlag" column="read_flag"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.eaglepoint.venue.domain.UserNotification" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_notification (username, message, notification_type, read_flag)
        VALUES (#{username}, #{message}, #{notificationType}, #{readFlag})
    </insert>

    <select id="findByUsername" parameterType="string" resultMap="UserNotificationMap">
        SELECT id, username, message, notification_type, read_flag, created_at
        FROM user_notification
        WHERE username = #{username}
        ORDER BY created_at DESC, id DESC
    </select>

    <select id="findById" parameterType="long" resultMap="UserNotificationMap">
        SELECT id, username, message, notification_type, read_flag, created_at
        FROM user_notification
        WHERE id = #{id}
    </select>

    <update id="markAsRead" parameterType="long">
        UPDATE user_notification
        SET read_flag = 'Y'
        WHERE id = #{id}
    </update>

    <update id="markAsReadByUsername">
        UPDATE user_notification
        SET read_flag = 'Y'
        WHERE id = #{id}
          AND username = #{username}
    </update>
</mapper>
