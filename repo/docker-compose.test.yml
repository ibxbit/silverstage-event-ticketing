services:
  test-runner:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: silverstage_test_runner
    environment:
      SPRING_PROFILES_ACTIVE: test
      APP_SECURITY_AES_KEY: test-key-material-1234567890-ABCD
    volumes:
      - ./target:/app/target
